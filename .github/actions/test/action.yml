name: Test
description: Test action

inputs:
  fail-on-error:
    description: 'Fail on error'
    required: true
    type: boolean

runs:
  using: "composite"
  steps:
    - shell: bash
      run:
        exit 1

    - shell: bash
      if: always() && inputs.fail-on-error
      run: echo Success!
