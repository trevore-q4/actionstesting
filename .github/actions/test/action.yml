name: Test
description: Test action

inputs:
  continue-on-error:
    description: 'Continue on error'
    required: true
    type: boolean

runs:
  using: "composite"
  steps:
    - shell: bash
      run:
        exit 1

    - shell: bash
      if: always() && inputs.continue-on-error == 'true'
      run: echo Success!
